{
  "name": "@segmento/waitlist-ui",
  "version": "0.7.1",
  "description": "Segmento waitlist modal — pure HTML/CSS/JS with shadow DOM",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@segmento/core": "workspace:*",
    "@segmento/lead": "workspace:*",
    "@segmento/solana": "workspace:*"
  },
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vite": "^5.2.0",
    "vite-plugin-dts": "^3.9.0"
  }
}
